<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
     <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "bootcamp app", root_path, id: "logo" %>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to t("menu.parkings"), parkings_path %></li>
        <% if current_person %>
          <li><%= link_to t("menu.cars"), cars_path %></li>
          <li><%= link_to t("menu.place_rents"), place_rents_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_person %>
          <li><%= link_to t("menu.log_out"), logout_path, method: :delete %></li>
        <% else %>
          <li><%= link_to t("menu.sign_up"), register_path %></li>
          <li><%= link_to t("menu.log_in"), login_path %></li>
        <% end %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= t("menu.language") %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t("menu.english"), url_for(params.merge(locale: :en)) %>
              </li>
              <li>
                <%= link_to t("menu.polish"), url_for(params.merge(locale: :pl)) %>
              </li>
            </ul>
          </li>
      </ul>
    </div>
  </div>
</nav>
